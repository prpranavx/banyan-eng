[build]
builder = "RAILPACK"
# Install dependencies + Playwright system libs, then build
installCommand = "npm install && npx playwright install-deps chromium"
buildCommand = "npm run build"

[deploy]
startCommand = "npm start"
